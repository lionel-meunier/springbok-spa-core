// Mocked object of the needed CONFIG global object for the test context
var CONFIG = {
    app: {
        sessionExpirationTimeInSeconds: 3600,
        logDebugEnabled: true,
        preferredLanguage: 'fr_FR'
    }
};

